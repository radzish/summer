name: experiments
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.4 <3.0.0'

dependencies:
  build:
  glob:
  analyzer:
  source_gen:
  build_runner_core:
  build_resolvers:
  example_extensions_lib:
    path: ../example_extensions/example_extensions_lib

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  example_extensions_generator:
    path: ../example_extensions/example_extensions_generator
